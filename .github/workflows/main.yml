name: Main
on:
  push:
    branches: [main]
    tags-ignore: ['**']

jobs:
  build:
    name: Build
    uses: ./.github/workflows/service.yml
    with:
      deploy: true
    permissions:
      pages: write
      id-token: write
